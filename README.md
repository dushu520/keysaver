# KeySaver 🔑

**KeySaver** 是一个现代化的、安全优先的桌面端密钥管理工具，专为开发者设计。它基于 Tauri 和 React 构建，提供极致的性能和原生体验，帮助你轻松管理各类 API Keys、Access Tokens 和 ID/Secret 组合。

## ✨ 核心特性

- **🛡️ 隐私优先**：所有数据仅存储在本地（`AppData` 目录），绝不上传云端，彻底杜绝数据泄露风险。
- **⚡️ 极速体验**：基于 Rust (Tauri) 构建，启动瞬间完成，内存占用极低。
- **🔄 双模支持**：
  - **API Key 模式**：适用于 OpenAI、Anthropic 等单字符串密钥。
  - **ID & Secret 模式**：适用于 AWS、阿里云、腾讯云等成对密钥。
- **💾 备份与恢复**：支持一键导出 JSON 备份文件，并可随时从备份文件恢复数据，确保数据安全。
- **🔍 智能管理**：
  - **即时搜索**：毫秒级全文检索。
  - **自动排序**：最新添加的密钥自动置顶。
  - **防重检测**：自动识别重复录入的密钥，避免冗余。
- **📋 便捷操作**：一键复制密钥到剪贴板，操作行云流水。

## 🛠️ 技术栈

本项目采用最新的前沿技术栈构建：

- **Core**: [Tauri v2](https://tauri.app/) (Rust) - 提供系统级能力与极小的包体积
- **Frontend**: [React 19](https://react.dev/) - 构建响应式用户界面
- **Language**: [TypeScript](https://www.typescriptlang.org/) - 类型安全保障
- **Styling**: [Tailwind CSS v4](https://tailwindcss.com/) - 现代原子化 CSS
- **State**: [Zustand](https://github.com/pmndrs/zustand) - 轻量级状态管理
- **Build**: [Vite](https://vitejs.dev/) - 极速构建工具

## 🚀 快速开始

### 环境要求

- **Node.js**: v18+
- **Rust**: 最新稳定版 (建议通过 `rustup` 安装)
- **包管理器**: npm, yarn 或 pnpm

### 本地开发

1. **克隆仓库**
   ```bash
   git clone https://github.com/dushu520/keysaver.git
   cd keysaver
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **启动开发环境**
   ```bash
   npm run tauri dev
   ```
   此命令将同时启动前端开发服务器和 Tauri 窗口。

### 📦 打包构建

构建适用于当前操作系统的生产包（Windows .exe / macOS .app / Linux .deb）：

```bash
npm run tauri build
```

构建产物将位于 `src-tauri/target/release/bundle/` 目录下。

## 📂 数据存储位置

- **Windows**: `C:\Users\<用户名>\AppData\Roaming\keysaver\keys.json`
- **macOS**: `~/Library/Application Support/keysaver/keys.json`
- **Linux**: `~/.local/share/keysaver/keys.json`

## 🤝 贡献

欢迎提交 Issue 或 Pull Request！

## 📄 许可证

MIT License
